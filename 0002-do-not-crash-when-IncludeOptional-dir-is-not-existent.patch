diff --git a/server/config.c b/server/config.c
index ffd26fd..16b4228 100644
--- a/server/config.c
+++ b/server/config.c
@@ -1930,8 +1930,11 @@ static const char *process_resource_config_nofnmatch(server_rec *s,
          */
         rv = apr_dir_open(&dirp, path, ptemp);
         if (rv != APR_SUCCESS) {
+            if (!optional)
             return apr_psprintf(p, "Could not open config directory %s: %pm",
                                 path, &rv);
+            else
+                return NULL;
         }
 
         candidates = apr_array_make(ptemp, 1, sizeof(fnames));
